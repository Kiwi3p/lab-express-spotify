
<div class="row">
        {{#each artistData}}
            <img src="{{images.0.url}}">
            <p>{{name}}</p>
            <div class="view-album">
            <a href="/albums/{{id}}">View tracks</a>
        </div>
        {{/each}}
</div>